export const PLATFORM_NOTIFICATION_EMAIL =
    process.env.NEXT_PUBLIC_PLATFORM_NOTIFICATION_EMAIL?.trim() || 'info@firmaone.com'

export const PLATFORM_SUPPORT_EMAIL =
    process.env.NEXT_PUBLIC_PLATFORM_SUPPORT_EMAIL?.trim() || 'support@firmaone.com'

